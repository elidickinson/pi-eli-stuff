{
  "arch": "aarch64",
  "distro": "alpine",
  "env": {
    "USE_BUILTIN_RIPGREP": "0",
    "DISABLE_AUTOUPDATER": "1",
    "DISABLE_TELEMETRY": "1",
    "BR_AUTOSTART": "1"
  },
  "alpine": {
    "version": "3.23.0",
    "kernelPackage": "linux-virt",
    "kernelImage": "vmlinuz-virt",
    "rootfsPackages": [
      "linux-virt",
      "rng-tools",
      "bash",
      "ca-certificates",
      "curl",
      "nodejs",
      "npm",
      "uv",
      "python3",
      "openssh",
      "libgcc",
      "libstdc++",
      "ripgrep",
      "git"
    ],
    "initramfsPackages": []
  },
  "rootfs": {
    "label": "gondolin-root"
  },
  "postBuild": {
    "commands": [
      "rm -rf /usr/lib/node_modules/npm && mkdir -p /usr/lib/node_modules/npm && curl -fsSL https://registry.npmjs.org/npm/-/npm-11.2.0.tgz | tar -xz -C /usr/lib/node_modules/npm --strip-components=1",
      "npm install -g @anthropic-ai/claude-code",
      "npm install -g --ignore-scripts @mariozechner/pi-coding-agent",
      "npm install -g https://github.com/elidickinson/browser-cli/archive/refs/heads/main.tar.gz",
      "mkdir -p /etc/skel/.claude && echo '{\"skipDangerousModePermissionPrompt\":true}' > /etc/skel/.claude/settings.json && echo '{\"hasCompletedOnboarding\":true,\"projects\":{\"/workspace\":{\"hasTrustDialogAccepted\":true,\"hasCompletedProjectOnboarding\":true,\"allowedTools\":[]}}}' > /etc/skel/.claude.json",
      "printf '#!/bin/sh\\ncp -rn /etc/skel/.claude /root/.claude 2>/dev/null; cp -n /etc/skel/.claude.json /root/.claude.json 2>/dev/null\\n' > /etc/profile.d/claude-init.sh && chmod +x /etc/profile.d/claude-init.sh",
      "printf '[safe]\\n\\tdirectory = *\\n' > /etc/gitconfig"
    ]
  }
}
